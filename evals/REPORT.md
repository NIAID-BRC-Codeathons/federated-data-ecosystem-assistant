# What these MCP servers add over reading the API docs

Generated by `evals/run_eval.py` against the live services.

Each case is run twice: the **baseline** is the obvious call, written the way
the API documentation leads you to write it, and the **tool** is our MCP tool.
Both are scored against a verified ground truth.

| | correct |
|---|---|
| baseline (raw API, written from the docs) | **0/10** |
| our MCP tools | **10/10** |
| cases where the tool fixes a wrong answer | **10/10** |

The failures below are not API outages. Every one returns HTTP 200 with a
plausible-looking answer, which is what makes them worth wrapping.

## Fetch the curated DataSet GDS5163

*A wrong prefix returns a real Affymetrix array design, with no error.*

| | result | correct |
|---|---|---|
| baseline | `esummary id=100005163 -> GPL5163` | **no** |
| tool | `geo_series('GDS5163') -> GDS5163` | yes |

Expected: `GDS5163` — fixes a silent wrong answer.

## Get the Entrez UID for sample GSM9284462

*gds indexes the accession inside every related record, so the Series ranks first.*

| | result | correct |
|---|---|---|
| baseline | `esearch term=GSM9284462 -> 3 hits, first=200309890` | **no** |
| tool | `geo_resolve_accession -> 309284462 (0 requests)` | yes |

Expected: `309284462` — fixes a silent wrong answer.

## How many E. coli GEO Series involve ciprofloxacin?

*Without a filter the count mixes Series, Platforms, Samples and DataSets.*

| | result | correct |
|---|---|---|
| baseline | `esearch without an entry-type filter -> 513` | **no** |
| tool | `geo_search(entry_type="gse") -> 37` | yes |

Expected: `37` — fixes a silent wrong answer.

## Where are the expression values for GSE309890?

*esummary names file FORMATS. The filenames and URLs live on the FTP host.*

| | result | correct |
|---|---|---|
| baseline | `esummary suppfile -> 'CSV' (a format, not a filename)` | **no** |
| tool | `geo_series -> 1 file(s): GSE309890_FPKMs_allSamples.csv.gz` | yes |

Expected: `GSE309890_FPKMs_allSamples.csv.gz` — fixes a silent wrong answer.

## List the E. coli GEO Series

*Default retmax is 20. The count is real; the id list is quietly cut.*

| | result | correct |
|---|---|---|
| baseline | `count=1929 but 20 ids returned, silently` | **no** |
| tool | `total_count=1929, returned=5, truncated=True` | yes |

Expected: `count and id list agree, or the truncation is stated` — fixes a silent wrong answer.

## Find E. coli sequencing runs whose study mentions resistance

*It sends an unquoted scientific_name to ENA; ENA answers HTTP 400.*

| | result | correct |
|---|---|---|
| baseline | `search_ena_keywords -> error text, no rows` | **no** |
| tool | `brc_ena_search -> total_matching=48,421` | yes |

Expected: `a usable row count` — fixes a silent wrong answer.

## How many sequencing runs does ENA hold for E. coli?

*50 with has_more is a page size. Reporting it as the answer is wrong by 4 orders of magnitude.*

| | result | correct |
|---|---|---|
| baseline | `search_ena -> count=50, has_more=True` | **no** |
| tool | `brc_ena_runs -> total_in_ena=551,679` | yes |

Expected: `a real total` — fixes a silent wrong answer.

## What organisms are actually in BioProject PRJNA715470?

*BRC's study endpoint 500s. The project is labelled E. coli and holds 13 genera-worth of runs.*

| | result | correct |
|---|---|---|
| baseline | `GET /api/v1/ena/study/PRJNA715470 -> HTTP 500` | **no** |
| tool | `brc_ena_study -> 369 runs, 13 distinct organisms` | yes |

Expected: `13` — fixes a silent wrong answer.

## How many E. coli isolates has NCBI Pathogen Detection sequenced?

*Pathogen Detection groups are curated, not taxonomy names. A wrong one returns 0, which reads as absence rather than as a bad query.*

| | result | correct |
|---|---|---|
| baseline | `taxgroup_name=="Escherichia coli" -> 0` | **no** |
| tool | `taxgroup_name=="E.coli and Shigella" -> 581,464` | yes |

Expected: `581464` — fixes a silent wrong answer.

## How many methicillin-resistant strains of E. coli are there?

*The board's own headline question. A refusal carries no information; the number does, and it points at the reframing.*

| | result | correct |
|---|---|---|
| baseline | `no number available; the question can only be declined` | **no** |
| tool | `mecA in E. coli = 2 of 581,464; in S. aureus = 93,260 of 171,412 -- the question is malformed, not the data missing` | yes |

Expected: `a number that shows the category is empty, plus a reframing` — fixes a silent wrong answer.
